import mongoose from "mongoose";
import getModels from "@/server/models/AAng/AAngLogistics";

const { Client } = await getModels(); // Load the Client model

class ClientController {

}

export default ClientController;